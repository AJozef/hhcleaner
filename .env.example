# Шаблон .env для hh_cleaner.
# Скопируй в .env и заполни нужные значения:
#   copy .env.example .env
#
# ВНИМАНИЕ: .env содержит пароль — он в .gitignore, никогда не коммить его.

# --- Авторизация (опционально) ---
# Если заданы:
#   1) при первом входе форма заполняется автоматически (капчу/2FA всё
#      равно проходите вручную в открытом окне);
#   2) при плановом --no-input прогоне, когда сессия истекла, скрипт
#      делает тихий headless-перелогин из этих данных, без открытия окна.
#      Если в этот момент hh.ru покажет капчу или 2FA — тихий перелогин
#      не справится, и придёт notification «нужен ручной вход».
# HH_EMAIL=your@email.com
# HH_PASSWORD=yourpassword

# Порог в днях для old-chats задаётся флагом: hhcleaner old-chats --days 30

# --- Каталог данных (опционально) ---
# Где хранить профиль браузера (сессию) и лог. По умолчанию ~/.hhcleaner.
# HHCLEANER_HOME=C:\path\to\hhcleaner-data
